function execute(username, steps, operation) {
	// Écrire le code ici
}

function forward(username, steps) {
	// Écrire le code ici
}

function backward(username, steps) {
	// Écrire le code ici
}

execute("BenBest", 1, forward); // "BenBest avance de 1."
execute("KingStar", 10, backward); // "KingStar recule de 10."